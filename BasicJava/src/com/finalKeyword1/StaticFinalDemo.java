package com.finalKeyword1;

public class StaticFinalDemo {
	static final int z;	// we can also declare static final variable
	 static{			// we can initialize in static block
		z=99;
	 }
// when we don't declare static keyword for block then we can't initialize static variable. 

//	public Static FinalDemo() //We can't initialize static final keyword in constructor or block.
//	{
//		z=88;
//	}

}
